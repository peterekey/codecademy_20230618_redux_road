const initialWagonState = {
    'supplies': 100,
    'distance': 0,
    'days': 0
}

const reducer = (state = initialWagonState, action) => {
    switch (action.payload) {
        default:
            return state
    }
}